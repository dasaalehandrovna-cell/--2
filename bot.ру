# ============================================
# Финансовый бот — версия 9.8.6 + Telegram Cloud Backup
# ============================================

import os
import json
import csv
import logging
import re
import threading
import time
from datetime import datetime
from zoneinfo import ZoneInfo

import requests
import telebot
from flask import Flask, request
from telebot import types

# --- Конфигурация ---
TOKEN = os.getenv("BOT_TOKEN")
OWNER_ID = os.getenv("OWNER_ID")
APP_URL = os.getenv("APP_URL", "https://yourapp.onrender.com")
TZ = ZoneInfo("America/Argentina/Catamarca")
DATA_FILE = "data.json"
CSV_FILE = "data.csv"
LOG_FILE = "log.txt"
PORT = int(os.environ.get("PORT", 5000))
VERSION = "9.8.6"
KEEP_ALIVE_INTERVAL_SECONDS = int(os.getenv("KEEP_ALIVE_INTERVAL_SECONDS", 13*60))
KEEP_ALIVE_SEND_TO_OWNER = True

if not TOKEN:
    raise ValueError("BOT_TOKEN не установлен!")

bot = telebot.TeleBot(TOKEN, threaded=True)
app = Flask(__name__)

# -----------------------------
# Авто-удаление сообщений
# -----------------------------
def send_temp_message(chat_id, text, delay=5, **kwargs):
    try:
        msg = bot.send_message(chat_id, text, **kwargs)
        threading.Timer(delay, lambda: delete_message_safe(chat_id, msg.message_id)).start()
        return msg
    except Exception as e:
        log_error(f"send_temp_message: {e}")

def delete_message_safe(chat_id, message_id):
    try:
        bot.delete_message(chat_id, message_id)
    except:
        pass

# -----------------------------
# Логирование
# -----------------------------
logging.basicConfig(
    level=logging.INFO,
    format="[{asctime}] {levelname}: {message}",
    style="{",
    handlers=[logging.FileHandler(LOG_FILE, encoding="utf-8"), logging.StreamHandler()]
)

def log_info(msg): logging.info(msg)
def log_error(msg): logging.error(msg)

log_info(f"Старт приложения. Версия {VERSION}")

# -----------------------------
# Работа с данными + Telegram Cloud Backup
# -----------------------------
def default_data():
    return {
        "overall_balance": 0,
        "records": [],
        "chats": {},
        "active_messages": {},
        "processed_messages": [],
        "forward_targets": [],
        "tracked_messages": {},
        "next_id": 1
    }

def load_data():
    """Загрузка локального файла и восстановление из Telegram Cloud при пустых данных."""
    if os.path.exists(DATA_FILE):
        try:
            with open(DATA_FILE, "r", encoding="utf-8") as f:
                d = json.load(f)
        except Exception as e:
            log_error(f"Ошибка чтения data.json: {e}")
            d = default_data()
    else:
        d = default_data()

    # Попытка загрузки из Telegram Cloud, если данных нет
    if not d.get("records"):
        try:
            cloud_msg_id_file = "cloud_backup_msg_id.txt"
            if os.path.exists(cloud_msg_id_file) and OWNER_ID:
                with open(cloud_msg_id_file, "r") as f:
                    msg_id = int(f.read().strip())
                f_msg = bot.forward_message(chat_id=int(OWNER_ID), from_chat_id=int(OWNER_ID), message_id=msg_id)
                cloud_data = f_msg.json if hasattr(f_msg, "json") else None
                if cloud_data:
                    d = json.loads(cloud_data)
                    log_info("Данные восстановлены из Telegram Cloud")
        except Exception as e:
            log_error(f"Ошибка восстановления из Cloud: {e}")

    # Дополняем недостающие ключи
    base = default_data()
    for k, v in base.items():
        if k not in d:
            d[k] = v
    return d

def save_data(d):
    """Сохраняем локально и в Telegram Cloud владельцу."""
    try:
        with open(DATA_FILE, "w", encoding="utf-8") as f:
            json.dump(d, f, ensure_ascii=False, indent=2)
        # --- Cloud Backup ---
        try:
            if OWNER_ID:
                tmp_file = "cloud_backup.json"
                with open(tmp_file, "w", encoding="utf-8") as f:
                    json.dump(d, f, ensure_ascii=False, indent=2)
                sent = bot.send_document(int(OWNER_ID), open(tmp_file, "rb"), caption="Авто бэкап данных")
                with open("cloud_backup_msg_id.txt", "w") as f:
                    f.write(str(sent.message_id))
                os.remove(tmp_file)
        except Exception as e:
            log_error(f"Ошибка Cloud Backup: {e}")
    except Exception as e:
        log_error(f"Ошибка сохранения data.json: {e}")

data = load_data()

# -----------------------------
# Поддержка старого OWNER_ID
# -----------------------------
if OWNER_ID:
    try:
        oi = int(OWNER_ID)
        if oi not in data.get("forward_targets", []):
            data["forward_targets"].append(oi)
            save_data(data)
    except Exception:
        pass
# ============================================
# Финансовый бот — Часть 2/4
# Функции работы с чатами и записями
# ============================================

# -----------------------------
# Работа с чат-стором
# -----------------------------
def get_chat_store(chat_id):
    """Возвращает словарь данных для конкретного чата"""
    if str(chat_id) not in data["chats"]:
        data["chats"][str(chat_id)] = {
            "balance": 0,
            "records": []
        }
    return data["chats"][str(chat_id)]

def add_record(chat_id, amount, note="", record_type="expense"):
    """Добавляет запись и сохраняет данные + Cloud Backup"""
    store = get_chat_store(chat_id)
    rid = data.get("next_id", 1)
    record = {
        "id": rid,
        "amount": amount,
        "note": note,
        "type": record_type,
        "date": datetime.now(TZ).isoformat()
    }
    store["records"].append(record)
    data["next_id"] = rid + 1

    # Обновление баланса
    if record_type == "expense":
        store["balance"] -= amount
        data["overall_balance"] -= amount
    else:
        store["balance"] += amount
        data["overall_balance"] += amount

    save_data(data)
    return record

def edit_record(chat_id, record_id, amount=None, note=None, record_type=None):
    """Редактирует запись по ID и сохраняет данные + Cloud Backup"""
    store = get_chat_store(chat_id)
    for rec in store["records"]:
        if rec["id"] == record_id:
            # Коррекция баланса
            if amount is not None:
                delta = amount - rec["amount"]
                if rec["type"] == "expense":
                    store["balance"] -= delta
                    data["overall_balance"] -= delta
                else:
                    store["balance"] += delta
                    data["overall_balance"] += delta
                rec["amount"] = amount
            if note is not None:
                rec["note"] = note
            if record_type is not None and record_type != rec["type"]:
                # Пересчёт баланса при смене типа
                if rec["type"] == "expense" and record_type == "income":
                    store["balance"] += rec["amount"] * 2
                    data["overall_balance"] += rec["amount"] * 2
                elif rec["type"] == "income" and record_type == "expense":
                    store["balance"] -= rec["amount"] * 2
                    data["overall_balance"] -= rec["amount"] * 2
                rec["type"] = record_type
            save_data(data)
            return rec
    return None

def reset_records(chat_id):
    """Обнуляет все записи чата"""
    store = get_chat_store(chat_id)
    # Коррекция общего баланса
    for rec in store["records"]:
        if rec["type"] == "expense":
            data["overall_balance"] += rec["amount"]
        else:
            data["overall_balance"] -= rec["amount"]
    store["records"] = []
    store["balance"] = 0
    save_data(data)

# -----------------------------
# Создание кнопок и меню
# -----------------------------
def main_keyboard():
    kb = types.ReplyKeyboardMarkup(resize_keyboard=True)
    kb.add("💰 Баланс", "➕ Добавить", "✏️ Редактировать", "🗂 Отчёт")
    return kb

def edit_keyboard(chat_id):
    kb = types.InlineKeyboardMarkup()
    kb.add(types.InlineKeyboardButton("Обнулить записи", callback_data=f"reset:{chat_id}"))
    kb.add(types.InlineKeyboardButton("Экспорт CSV", callback_data=f"csv:{chat_id}"))
    kb.add(types.InlineKeyboardButton("Назад", callback_data="back"))
    return kb

# -----------------------------
# Примеры команд
# -----------------------------
@bot.message_handler(commands=["start"])
def cmd_start(msg):
    chat_id = msg.chat.id
    bot.send_message(chat_id, f"Привет! Финансовый бот v{VERSION}", reply_markup=main_keyboard())
    # Показываем баланс при старте
    store = get_chat_store(chat_id)
    bot.send_message(chat_id, f"Ваш текущий баланс: {store['balance']:.2f}")

@bot.message_handler(func=lambda m: m.text == "💰 Баланс")
def show_balance(msg):
    chat_id = msg.chat.id
    store = get_chat_store(chat_id)
    bot.send_message(chat_id, f"Ваш текущий баланс: {store['balance']:.2f}")

@bot.message_handler(func=lambda m: m.text == "➕ Добавить")
def add_new_record(msg):
    chat_id = msg.chat.id
    bot.send_message(chat_id, "Введите сумму и описание через запятую. Пример: 500, обед")
    bot.register_next_step_handler(msg, process_new_record)

def process_new_record(msg):
    chat_id = msg.chat.id
    try:
        text = msg.text
        parts = [p.strip() for p in text.split(",")]
        amount = float(parts[0])
        note = parts[1] if len(parts) > 1 else ""
        rec = add_record(chat_id, amount, note, "expense")
        bot.send_message(chat_id, f"Добавлена запись: {rec['amount']} — {rec['note']}")
    except Exception as e:
        send_temp_message(chat_id, f"Ошибка: {e}")

# -----------------------------
# Обработка Inline кнопок
# -----------------------------
@bot.callback_query_handler(func=lambda call: True)
def callback_handler(call):
    data_call = call.data
    chat_id = call.message.chat.id

    if data_call.startswith("reset:"):
        reset_records(chat_id)
        bot.edit_message_text("Все записи обнулены.", chat_id, call.message.message_id)
    elif data_call.startswith("csv:"):
        export_csv(chat_id, call.message.message_id)
    elif data_call == "back":
        bot.edit_message_text("Меню редактирования:", chat_id, call.message.message_id, reply_markup=edit_keyboard(chat_id))
# ============================================
# Финансовый бот — Часть 3/4
# Экспорт, форвард, уведомления и keep-alive
# ============================================

import csv
import io
import threading
import time

OWNER_ID = 123456789  # ID владельца бота

# -----------------------------
# Экспорт CSV
# -----------------------------
def export_csv(chat_id, message_id=None):
    store = get_chat_store(chat_id)
    output = io.StringIO()
    writer = csv.writer(output)
    writer.writerow(["ID", "Дата", "Сумма", "Тип", "Описание"])
    for rec in store["records"]:
        writer.writerow([rec["id"], rec["date"], rec["amount"], rec["type"], rec["note"]])
    output.seek(0)

    bot.send_document(chat_id, ("report.csv", output.getvalue()))
    if message_id:
        bot.edit_message_text("CSV экспортирован.", chat_id, message_id)

# -----------------------------
# Форвард сообщений владельцу
# -----------------------------
def forward_to_owner(msg):
    try:
        bot.forward_message(OWNER_ID, msg.chat.id, msg.message_id)
    except Exception as e:
        print(f"Ошибка форварда владельцу: {e}")

# -----------------------------
# Уведомление владельца при новых записях
# -----------------------------
def notify_owner_new_record(chat_id, record):
    text = f"Новая запись в чате {chat_id}: {record['amount']} — {record['note']}"
    try:
        bot.send_message(OWNER_ID, text)
    except Exception as e:
        print(f"Ошибка уведомления владельца: {e}")

# -----------------------------
# Keep-Alive (пинг бота каждые X секунд)
# -----------------------------
def keep_alive(interval=600):
    """Отправляет пинг владельцу каждые interval секунд для проверки работы бота"""
    def ping():
        while True:
            try:
                bot.send_message(OWNER_ID, "💓 Keep-Alive: бот работает")
            except Exception as e:
                print(f"Ошибка Keep-Alive: {e}")
            time.sleep(interval)
    t = threading.Thread(target=ping, daemon=True)
    t.start()

# -----------------------------
# Интеграция с добавлением записей
# -----------------------------
def add_record_notify(chat_id, amount, note="", record_type="expense"):
    """Добавляет запись + уведомление владельца + бэкап"""
    rec = add_record(chat_id, amount, note, record_type)
    notify_owner_new_record(chat_id, rec)
    return rec
# ============================================
# Финансовый бот — Часть 4/4
# Авто-бэкап в Telegram, восстановление, запуск
# ============================================

import json

BACKUP_CHAT_ID = OWNER_ID  # Используем OWNER_ID для хранения бэкапа
BACKUP_FILE_NAME = "backup_data.json"

# -----------------------------
# Сохранение бэкапа в телеграм
# -----------------------------
def backup_to_telegram():
    try:
        json_data = json.dumps(data, ensure_ascii=False, indent=2)
        bot.send_document(BACKUP_CHAT_ID, (BACKUP_FILE_NAME, json_data.encode("utf-8")))
        print("✅ Бэкап отправлен в Telegram")
    except Exception as e:
        print(f"❌ Ошибка бэкапа в Telegram: {e}")

# -----------------------------
# Восстановление данных из последнего бэкапа
# -----------------------------
def restore_from_telegram():
    """Ищет последнее сообщение с бэкапом в чате OWNER_ID"""
    try:
        messages = bot.get_chat_history(BACKUP_CHAT_ID, limit=20)
        for msg in reversed(messages):
            if msg.document and msg.document.file_name == BACKUP_FILE_NAME:
                file_info = bot.get_file(msg.document.file_id)
                downloaded_file = bot.download_file(file_info.file_path)
                d = json.loads(downloaded_file.decode("utf-8"))
                global data
                data = d
                save_data(data)
                print("✅ Данные восстановлены из Telegram")
                return True
    except Exception as e:
        print(f"❌ Ошибка восстановления из Telegram: {e}")
    return False

# -----------------------------
# Авто-бэкап при изменении данных
# -----------------------------
def auto_backup_on_change():
    """Следит за изменения данных и отправляет бэкап каждые X секунд"""
    INTERVAL = 300  # каждые 5 минут
    last_snapshot = json.dumps(data, ensure_ascii=False)
    def task():
        nonlocal last_snapshot
        while True:
            try:
                current_snapshot = json.dumps(data, ensure_ascii=False)
                if current_snapshot != last_snapshot:
                    backup_to_telegram()
                    last_snapshot = current_snapshot
            except Exception as e:
                print(f"❌ Ошибка авто-бэкапа: {e}")
            time.sleep(INTERVAL)
    t = threading.Thread(target=task, daemon=True)
    t.start()

# -----------------------------
# Установка webhook (для Render, Railway, и т.д.)
# -----------------------------
def set_webhook():
    """Устанавливает webhook, если бот работает на сервере с HTTPS"""
    webhook_url = os.getenv("RENDER_EXTERNAL_URL")
    if webhook_url:
        full_url = f"{webhook_url}/{TOKEN}"
        try:
            bot.remove_webhook()
            bot.set_webhook(url=full_url)
            log_info(f"Webhook установлен: {full_url}")
        except Exception as e:
            log_error(f"Ошибка установки webhook: {e}")
    else:
        log_info("Webhook не используется — работает polling")

# Вызываем установку
set_webhook()

# -----------------------------
# Старт бота с восстановлением
# -----------------------------
if __name__ == "__main__":
    print("⏳ Старт бота, попытка восстановления данных...")
    restored = restore_from_telegram()
    if not restored:
        print("⚠️ Данных из Telegram не найдено, используем локальные данные.")
    
    # Запуск keep-alive и авто-бэкапа
    keep_alive(interval=KEEP_ALIVE_INTERVAL_SECONDS)
    auto_backup_on_change()
    
    # Webhook и Flask
    set_webhook()
    schedule_daily_window_creation()
    
    print(f"🚀 Бот ФО версии {VERSION} запущен")
    try:
        bot.send_message(OWNER_ID, f"✅ Бот запущен и работает! (версия {VERSION})")
    except Exception as e:
        print(f"❌ Не удалось уведомить владельца о старте: {e}")
    
    # Flask приложение
    app.run(host="0.0.0.0", port=PORT)